# revealOnScroll.js

Requirement:
jQuery
Waypoints.js

To use:
new RevealOnScroll(selector, offsetPercentage);


Example:
class
var section = new RevealOnScroll('.section-item', "60%");

id
var section = new RevealOnScroll('#section-item', "60%");
